// const Joi = require('joi');

// Const UserSchema = new Schema(
//     {
//         email: Joi.string().require(),
//         first_name: Joi.string().require(),
//         last_name: Joi.string().require(),
//         first_name: Joi.string().require(),
//     }
// )